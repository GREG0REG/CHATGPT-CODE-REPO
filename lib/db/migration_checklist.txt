================================================================================
DATABASE MIGRATION CHECKLIST — event_countdown.db (Version 21)
================================================================================
Verify every step below was preserved in database_helper.dart onUpgrade().

□ V1 → V2  (events table)
  [ ] ALTER TABLE events ADD COLUMN recurrence INTEGER DEFAULT 0
  [ ] ALTER TABLE events ADD COLUMN recurrenceInterval INTEGER DEFAULT 1
  [ ] ALTER TABLE events ADD COLUMN yearlyUseSpecificDates INTEGER DEFAULT 0
  [ ] ALTER TABLE events ADD COLUMN yearlySpecificDatesJson TEXT
  [ ] ALTER TABLE events ADD COLUMN excludedDatesJson TEXT

□ V2 → V3  (custom_reminders table)
  [ ] ALTER TABLE custom_reminders ADD COLUMN isEnabled INTEGER DEFAULT 1

□ V3 → V4  (events table)
  [ ] ALTER TABLE events ADD COLUMN iconName TEXT
  [ ] ALTER TABLE events ADD COLUMN priority INTEGER DEFAULT 2
  [ ] ALTER TABLE events ADD COLUMN subjectTag TEXT
  [ ] ALTER TABLE events ADD COLUMN isCompleted INTEGER DEFAULT 0

□ V4 → V5  (NEW TABLES)
  [ ] CREATE TABLE study_sessions (id, eventId, subjectTag, durationMinutes, completedAtMillis, sessionType, notes, distractionCount, intensityRating, topicTag)
  [ ] CREATE TABLE subtasks (id, eventId, title, isCompleted, orderIndex)
  [ ] CREATE TABLE flashcards (id, subjectTag, frontText, backText, boxLevel, lastReviewedMillis, nextReviewMillis)
  [ ] CREATE TABLE study_schedules (id, eventId, subjectTag, suggestedDateMillis, suggestedDurationMinutes, isCompleted, isAccepted)
  [ ] CREATE TABLE daily_goals (id, dateMillis UNIQUE, targetMinutes, targetPomodoros, achievedMinutes, achievedPomodoros, streakCount)

□ V5 → V6  (NEW TABLE)
  [ ] CREATE TABLE study_subjects (id, name UNIQUE, colorHex, totalFocusMinutes, createdAtMillis)

□ V6 → V7  (study_sessions table)
  [ ] ALTER TABLE study_sessions ADD COLUMN notes TEXT

□ V7 → V8  (NEW TABLES)
  [ ] CREATE TABLE flashcard_review_history (id, cardId, reviewedAtMillis, difficulty, timeSpentSeconds, boxLevelBefore, boxLevelAfter, sessionId)
  [ ] CREATE TABLE daily_card_goals (id, dateMillis UNIQUE, targetReviews, achievedReviews, targetNewCards, achievedNewCards, streakCount)
  [ ] CREATE TABLE grade_components (id, name, weight, score, totalPoints, createdAtMillis)
  [ ] CREATE TABLE quick_notes (id, title, content, subject, createdAtMillis, updatedAtMillis)

□ V8 → V9  (flashcards table)
  [ ] ALTER TABLE flashcards ADD COLUMN imagePath TEXT
  [ ] ALTER TABLE flashcards ADD COLUMN audioFrontPath TEXT
  [ ] ALTER TABLE flashcards ADD COLUMN audioBackPath TEXT
  [ ] ALTER TABLE flashcards ADD COLUMN tagsJson TEXT DEFAULT "[]"
  [ ] ALTER TABLE flashcards ADD COLUMN totalReviews INTEGER DEFAULT 0
  [ ] ALTER TABLE flashcards ADD COLUMN consecutiveCorrect INTEGER DEFAULT 0
  [ ] ALTER TABLE flashcards ADD COLUMN createdAtMillis INTEGER
  [ ] ALTER TABLE flashcards ADD COLUMN isFavorite INTEGER DEFAULT 0
  [ ] ALTER TABLE flashcards ADD COLUMN difficultyRating REAL DEFAULT 2.5
  [ ] ALTER TABLE flashcards ADD COLUMN reviewHistoryJson TEXT
  [ ] UPDATE flashcards SET createdAtMillis = <now> WHERE createdAtMillis IS NULL

□ V9 → V10  (NEW TABLE)
  [ ] CREATE TABLE attendance_logs (id, subjectName, dateMillis, status, note, createdAtMillis)

□ V10 → V11  (attendance_logs + NEW TABLES)
  [ ] ALTER TABLE attendance_logs ADD COLUMN subjectId INTEGER
  [ ] ALTER TABLE attendance_logs ADD COLUMN scheduleId INTEGER
  [ ] ALTER TABLE attendance_logs ADD COLUMN markedAtMillis INTEGER
  [ ] ALTER TABLE attendance_logs ADD COLUMN isAutoGenerated INTEGER DEFAULT 0
  [ ] CREATE TABLE attendance_subjects (id, name UNIQUE, requiredPercentage, maxAllowedAbsences, semesterStartMillis, semesterEndMillis, colorHex, createdAtMillis)
  [ ] CREATE TABLE attendance_schedules (id, subjectId, dayOfWeek, startTimeMinutes, endTimeMinutes, room, professor, scheduleType, isActive)
  [ ] CREATE TABLE timetable_classes (id, subjectName, classType, dayOfWeek, startTimeMinutes, endTimeMinutes, room, professor, colorHex, isRecurring, startDateMillis, endDateMillis, note, createdAtMillis)
  [ ] CREATE TABLE timetable_tasks (id, title, taskType, subjectName, dueDateMillis, startTimeMinutes, endTimeMinutes, isAllDay, colorHex, isCompleted, reminderMinutes, note, createdAtMillis)
  [ ] CREATE TABLE academic_calendar (id, dateMillis, name, type, isRecurringYearly)

□ V11 → V12  (NEW TABLES)
  [ ] CREATE TABLE class_schedule (id, subjectName, dayOfWeek, startTimeMinutes, endTimeMinutes, room, colorHex, isActive)
  [ ] CREATE TABLE habits (id, name, subjectName, colorHex, targetPerWeek, reminderTimeMinutes, createdAtMillis, isArchived)
  [ ] CREATE TABLE habit_logs (id, habitId, dateMillis, completed, note)
  [ ] CREATE TABLE reading_books (id, title, author, totalPages, currentPage, subjectName, colorHex, startDateMillis, targetEndDateMillis, dailyPageGoal, minutesReadToday, totalMinutesRead, isCompleted, createdAtMillis)

□ V12 → V13  (habits + habit_logs tables)
  [ ] ALTER TABLE habits ADD COLUMN habitType INTEGER DEFAULT 0
  [ ] ALTER TABLE habits ADD COLUMN metricGoal INTEGER
  [ ] ALTER TABLE habits ADD COLUMN unitLabel TEXT
  [ ] ALTER TABLE habit_logs ADD COLUMN metricValue INTEGER

□ V13 → V14  (NEW TABLE)
  [ ] CREATE TABLE reading_sessions (id, bookId, startPage, endPage, pagesRead, minutesRead, pagesPerMinute, sessionDateMillis, note, createdAtMillis)

□ V14 → V15  (study_sessions table)
  [ ] ALTER TABLE study_sessions ADD COLUMN distractionCount INTEGER DEFAULT 0
  [ ] ALTER TABLE study_sessions ADD COLUMN intensityRating INTEGER DEFAULT 0
  [ ] ALTER TABLE study_sessions ADD COLUMN topicTag TEXT

□ V15 → V16  (events + study_sessions tables + NEW TABLE)
  [ ] ALTER TABLE events ADD COLUMN targetScore INTEGER
  [ ] ALTER TABLE events ADD COLUMN neetExamType TEXT
  [ ] ALTER TABLE events ADD COLUMN revisionRound TEXT
  [ ] ALTER TABLE events ADD COLUMN isPyqSession INTEGER DEFAULT 0
  [ ] ALTER TABLE events ADD COLUMN difficulty TEXT
  [ ] ALTER TABLE events ADD COLUMN studyDuration TEXT
  [ ] ALTER TABLE events ADD COLUMN studyModeTagsJson TEXT
  [ ] ALTER TABLE study_sessions ADD COLUMN neetSubject INTEGER
  [ ] ALTER TABLE study_sessions ADD COLUMN mcqsAttempted INTEGER DEFAULT 0
  [ ] ALTER TABLE study_sessions ADD COLUMN mcqsCorrect INTEGER DEFAULT 0
  [ ] ALTER TABLE study_sessions ADD COLUMN difficultyLevel INTEGER DEFAULT 1
  [ ] ALTER TABLE study_sessions ADD COLUMN revisionRound INTEGER DEFAULT 0
  [ ] ALTER TABLE study_sessions ADD COLUMN mockTestScore INTEGER
  [ ] ALTER TABLE study_sessions ADD COLUMN mockTestRank INTEGER
  [ ] CREATE TABLE gpa_courses (id, name, credits, grade, gradePoints, semester, subjectCategory, createdAtMillis)

□ V16 → V17  (events table + NEW SYLLABUS TABLES)
  [ ] ALTER TABLE events ADD COLUMN assignmentType TEXT
  [ ] CREATE TABLE syllabus_subjects (id, name, colorHex, targetCompletionDateMillis, createdAtMillis)
  [ ] CREATE TABLE syllabus_units (id, subjectId, name, orderIndex, weightage, createdAtMillis)
  [ ] CREATE TABLE syllabus_topics (id, unitId, name, orderIndex, status, difficulty, estimatedMinutes, createdAtMillis)
  [ ] CREATE TABLE syllabus_subtopics (id, topicId, name, orderIndex, status, notes, createdAtMillis)
  [ ] CREATE TABLE syllabus_resources (id, topicId, subtopicId, resourceType, title, filePath, url, createdAtMillis)
  [ ] CREATE TABLE syllabus_study_links (id, topicId, studySessionId, createdAtMillis)
  [ ] CREATE TABLE syllabus_revision_schedules (id, topicId, revisionNumber, scheduledDateMillis, isCompleted, createdAtMillis)
  [ ] CREATE TABLE study_plans (id, name, eventId, subjectId, startDateMillis, endDateMillis, dailyStudyMinutes, isActive, createdAtMillis)
  [ ] CREATE TABLE study_plan_items (id, planId, topicId, scheduledDateMillis, allocatedMinutes, isCompleted, notes, createdAtMillis)

□ V17 → V18  (quick_notes table)
  [ ] ALTER TABLE quick_notes ADD COLUMN tagsJson TEXT DEFAULT '[]'
  [ ] ALTER TABLE quick_notes ADD COLUMN isPinned INTEGER DEFAULT 0
  [ ] ALTER TABLE quick_notes ADD COLUMN isArchived INTEGER DEFAULT 0
  [ ] ALTER TABLE quick_notes ADD COLUMN noteColor TEXT DEFAULT '#2D2D2D'

□ V18 → V19  (study_plans + study_plan_items + syllabus_topics tables)
  [ ] ALTER TABLE study_plans ADD COLUMN priority INTEGER DEFAULT 2
  [ ] ALTER TABLE study_plans ADD COLUMN strategy TEXT DEFAULT 'balanced'
  [ ] ALTER TABLE study_plans ADD COLUMN bufferDays INTEGER DEFAULT 7
  [ ] ALTER TABLE study_plan_items ADD COLUMN orderIndex INTEGER DEFAULT 0
  [ ] ALTER TABLE syllabus_topics ADD COLUMN neetMarksWeightage INTEGER

□ V19 → V20  (syllabus tables + NEW TABLES)
  [ ] ALTER TABLE syllabus_topics ADD COLUMN notes TEXT
  [ ] ALTER TABLE syllabus_topics ADD COLUMN targetCompletionDateMillis INTEGER
  [ ] ALTER TABLE syllabus_topics ADD COLUMN mcqsAttempted INTEGER DEFAULT 0
  [ ] ALTER TABLE syllabus_topics ADD COLUMN mcqsCorrect INTEGER DEFAULT 0
  [ ] ALTER TABLE syllabus_topics ADD COLUMN lastMockScore INTEGER
  [ ] ALTER TABLE syllabus_topics ADD COLUMN bestMockScore INTEGER
  [ ] ALTER TABLE syllabus_topics ADD COLUMN totalStudyMinutes INTEGER DEFAULT 0
  [ ] ALTER TABLE syllabus_subjects ADD COLUMN examCategory TEXT
  [ ] ALTER TABLE syllabus_subjects ADD COLUMN totalMarksWeightage INTEGER
  [ ] ALTER TABLE syllabus_revision_schedules ADD COLUMN performanceScore INTEGER
  [ ] ALTER TABLE syllabus_revision_schedules ADD COLUMN actualRevisionDateMillis INTEGER
  [ ] ALTER TABLE syllabus_revision_schedules ADD COLUMN notes TEXT
  [ ] CREATE TABLE mock_test_history (id, subjectId, testName, score, totalMarks, rank, totalStudents, topicsTested, dateMillis, notes, createdAtMillis)
  [ ] CREATE TABLE chapter_deadlines (id, topicId UNIQUE, targetDateMillis, priority, reminderDays, isCompleted, createdAtMillis)

□ V20 → V21  (study_sessions table)
  [ ] ALTER TABLE study_sessions ADD COLUMN productivity INTEGER DEFAULT 7

================================================================================
COLUMN VERIFICATION — All tables in onCreate()
================================================================================
□ events: id, title, dateMillis, startTimeMillis, deadlineMillis, notes,
          recurrence, recurrenceInterval, yearlyUseSpecificDates,
          yearlySpecificDatesJson, excludedDatesJson, iconName, priority,
          subjectTag, isCompleted, isNeetExam, neetTotalMarks, neetTargetScore,
          neetSubjectFocus, targetScore, neetExamType, revisionRound,
          isPyqSession, difficulty, studyDuration, studyModeTagsJson

□ custom_reminders: id, eventId, minutesBefore, type, soundUri, isEnabled

□ notification_history: id, eventId, eventTitle, reminderType, sentAtMillis

□ study_sessions: id, eventId, subjectTag, durationMinutes, completedAtMillis,
                  sessionType, notes, distractionCount, intensityRating,
                  topicTag, neetSubject, mcqsAttempted, mcqsCorrect,
                  difficultyLevel, revisionRound, mockTestScore, mockTestRank,
                  productivity

□ subtasks: id, eventId, title, isCompleted, orderIndex

□ flashcards: id, subjectTag, frontText, backText, boxLevel, lastReviewedMillis,
              nextReviewMillis, imagePath, audioFrontPath, audioBackPath,
              tagsJson, totalReviews, consecutiveCorrect, createdAtMillis,
              isFavorite, difficultyRating, reviewHistoryJson

□ study_schedules: id, eventId, subjectTag, suggestedDateMillis,
                   suggestedDurationMinutes, isCompleted, isAccepted

□ daily_goals: id, dateMillis, targetMinutes, targetPomodoros,
               achievedMinutes, achievedPomodoros, streakCount

□ study_subjects: id, name, colorHex, totalFocusMinutes, createdAtMillis

□ flashcard_review_history: id, cardId, reviewedAtMillis, difficulty,
                            timeSpentSeconds, boxLevelBefore, boxLevelAfter,
                            sessionId

□ daily_card_goals: id, dateMillis, targetReviews, achievedReviews,
                    targetNewCards, achievedNewCards, streakCount

□ grade_components: id, name, weight, score, totalPoints, createdAtMillis

□ gpa_courses: id, name, credits, grade, gradePoints, semester,
               subjectCategory, createdAtMillis

□ quick_notes: id, title, content, subject, tagsJson, isPinned, isArchived,
               noteColor, createdAtMillis, updatedAtMillis

□ attendance_logs: id, subjectName, subjectId, scheduleId, dateMillis, status,
                   note, markedAtMillis, isAutoGenerated, createdAtMillis

□ attendance_subjects: id, name, requiredPercentage, maxAllowedAbsences,
                       semesterStartMillis, semesterEndMillis, colorHex,
                       createdAtMillis

□ attendance_schedules: id, subjectId, dayOfWeek, startTimeMinutes,
                        endTimeMinutes, room, professor, scheduleType, isActive

□ timetable_classes: id, subjectName, classType, dayOfWeek, startTimeMinutes,
                     endTimeMinutes, room, professor, colorHex, isRecurring,
                     startDateMillis, endDateMillis, note, createdAtMillis

□ timetable_tasks: id, title, taskType, subjectName, dueDateMillis,
                   startTimeMinutes, endTimeMinutes, isAllDay, colorHex,
                   isCompleted, reminderMinutes, note, createdAtMillis

□ academic_calendar: id, dateMillis, name, type, isRecurringYearly

□ class_schedule: id, subjectName, dayOfWeek, startTimeMinutes, endTimeMinutes,
                  room, colorHex, isActive

□ habits: id, name, subjectName, colorHex, targetPerWeek, reminderTimeMinutes,
          createdAtMillis, isArchived, habitType, metricGoal, unitLabel

□ habit_logs: id, habitId, dateMillis, completed, note, metricValue

□ reading_books: id, title, author, totalPages, currentPage, subjectName,
                 colorHex, startDateMillis, targetEndDateMillis, dailyPageGoal,
                 minutesReadToday, totalMinutesRead, isCompleted, createdAtMillis

□ reading_sessions: id, bookId, startPage, endPage, pagesRead, minutesRead,
                    pagesPerMinute, sessionDateMillis, note, createdAtMillis

□ syllabus_subjects: id, name, colorHex, targetCompletionDateMillis,
                     totalMarksWeightage, examCategory, createdAtMillis

□ syllabus_units: id, subjectId, name, orderIndex, weightage, createdAtMillis

□ syllabus_topics: id, unitId, name, orderIndex, status, difficulty,
                   estimatedMinutes, neetMarksWeightage,
                   targetCompletionDateMillis, mcqsAttempted, mcqsCorrect,
                   lastMockScore, bestMockScore, totalStudyMinutes,
                   createdAtMillis

□ syllabus_subtopics: id, topicId, name, orderIndex, status, notes,
                      createdAtMillis

□ syllabus_resources: id, topicId, subtopicId, resourceType, title, filePath,
                      url, createdAtMillis

□ syllabus_study_links: id, topicId, studySessionId, createdAtMillis

□ syllabus_revision_schedules: id, topicId, revisionNumber,
                               scheduledDateMillis, isCompleted,
                               performanceScore, actualRevisionDateMillis,
                               notes, createdAtMillis

□ study_plans: id, name, eventId, subjectId, startDateMillis, endDateMillis,
               dailyStudyMinutes, isActive, priority, strategy, bufferDays,
               createdAtMillis

□ study_plan_items: id, planId, topicId, scheduledDateMillis, allocatedMinutes,
                    isCompleted, notes, orderIndex, createdAtMillis

□ mock_test_history: id, subjectId, testName, score, totalMarks, rank,
                     totalStudents, topicsTested, dateMillis, notes,
                     createdAtMillis

□ chapter_deadlines: id, topicId, targetDateMillis, priority, reminderDays,
                     isCompleted, createdAtMillis

================================================================================
EOF
